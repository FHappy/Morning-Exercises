// Given 2 ints, int1 and int2, return their sum. 
// However, sums in the range 10..19 inclusive, are forbidden,
//  ...so in that case just return 20.

// EXAMPLES:
// sortaSum(3, 4) → 7
// sortaSum(9, 4) → 20
// sortaSum(10, 11) → 21

// IMPORTANT: Do not change the function name

function sortaSum(int1, int2) {
  // ENTER ANSWER HERE
}

module.exports = sortaSum;