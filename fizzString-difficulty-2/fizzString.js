// Given a string str, if the string starts with "f" return "Fizz". 
// If the string ends with "b" return "Buzz". 
// If the string starts with f AND ends with b, return "FizzBuzz" 
// In all other cases, return the string unchanged

//note: cases sensitivity matters, the tests will fail if you return 'fizz'
// instead of 'Fizz', or 'buzz' instead of 'Buzz'

// EXAMPLES:
// fizzString("fig") → "Fizz"
// fizzString("dib") → "Buzz"
// fizzString("fib") → "FizzBuzz"

// IMPORTANT: Do not change the function name

function fizzString(str) {
// ENTER ANSWER BELOW
}

module.exports = fizzString;