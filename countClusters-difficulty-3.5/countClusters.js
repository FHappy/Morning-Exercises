// Given an array of integers, we'll say a cluster is 2 or more consecutive integers of the same value.
// Return the number of clusters

// EXAMPLES:
// countClusters([1, 2, 2, 3, 4, 4]) → 2  =>clusters are 2,2 and 4,4
// countClusters([1, 1, 2, 1, 1]) → 2     => clusters are 1,1 and 1,1
// countClusters([1, 1, 1, 1, 1]) → 1     => cluster is 1,1,1,1,1

function countClusters(arr) {
  // ENTER ANSWER BELOW
}

module.exports = countClusters;