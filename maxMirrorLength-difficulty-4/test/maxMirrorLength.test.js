var expect = require('chai').expect;
var maxMirrorLength = require('./../maxMirrorLength.js');

describe('maxMirrorLength', function() {
  describe("#empty array", function() {
    it('should return 0 from an empty array', function() {
      expect(maxMirrorLength([])).to.equal(0);
    });
  });

  describe("#no clusters in nonempty array", function() {
    it('should return 0 from ')
  })
});