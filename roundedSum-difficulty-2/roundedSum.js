// Given 3 non-negative integers, return the sum of the rounded values of each integer.
// We're rounding to the nearest ten btw. 75 rounds to 80, 73 rounds to 70, etc.
// To make this uber dry, you could make a function inside our roundedSum function
// that takes an integer argument and returns the rounded value.
// Why does this help dry up the code?

// EXAMPLES
// roundedSum(16, 17, 18) → 60
// roundedSum(12, 13, 14) → 30
// roundedSum(6, 4, 4) → 10

function roundedSum(int1, int2, int3) {
  // ENTER CODE BELOW
}

module.exports = roundedSum;